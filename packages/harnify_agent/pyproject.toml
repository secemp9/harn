[project]
name = "harnify-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "harnify-ai",
    "pydantic>=2.10",
    "aiofiles>=24.1",
    "ruamel.yaml>=0.18",
    "jsonlines>=4.0",
    "httpx>=0.28",
    "pathspec>=0.12",
]

[tool.uv.sources]
harnify-ai = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/harnify_agent"]
